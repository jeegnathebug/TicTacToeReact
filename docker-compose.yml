version: '3.4'

services:
    web:
        container_name: web
        build:
            context: .
        ports:
            - "8080:8080"
        volumes:
            - ./:/code
        environment:
            - NODE_ENV=development
        command: [ "yarn", "start:local" ]
